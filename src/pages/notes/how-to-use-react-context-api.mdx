---
title: How to use React context API
createdAt: 2020-11-11T15:12:32.761Z
tags:
  - React
  - JavaScript
  - TypeScript
---

# How to use React context API

This is the very beginning of "How to use React context API".

```shell script
alias dc="docker-compose"
alias dcu="docker-compose up"
alias dcud="docker-compose up -d"
alias dcl="docker-compose logs -f --tail 100"
alias dce="docker-compose exec"
alias dcps="docker-compose ps"
alias dcs="docker-compose stop"
```

```tsx [5-7]
import styled from 'styled-components'
import { FC } from 'react'
import { Link } from 'gatsby'

const StyledButton = styled.button`
  color: palevioletred;
  border-radius: 3px;
`

export const SomeComponent: FC = ({ children }) => {
  return (
    <StyledButton as={Link} href={'/home'}>
      {children}
    </StyledButton>
  )
}
```
